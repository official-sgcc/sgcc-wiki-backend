# 권한 점검 — 2026-09-19

프론트·백엔드 `refactor/permission-policy` 작업 트리 기준. 운영 서버/원격 DB를 변경하거나 침투 테스트하지 않았으며, 코드 검사와 로컬 회귀 테스트 결과다.

## 최종 정책 (사용자 확인 반영)

- 카테고리 생성·수정·삭제, 태그 삭제, 사용자 관리, 문서 카테고리 이동은 실제 admin 전용이다.
- 관리자는 권한 검사에서 우선 통과한다. 대상 존재 여부, 중복 제목, 순환 참조 등의 데이터 무결성 검증은 유지한다.
- 문서별 ACL을 복구해 공통 등급 검사와 연동한다. 수정는 club_member 이상 + 문서별 목록 + 카테고리 상속 제한을 적용한다. 기존 목록은 변경하지 않는다.
- 일반 회원은 허용된 카테고리에서 신규 작성 가능하지만 수정·제목 변경은 동아리 회원 이상이다.
- 자신과 모든 조상의 가장 높은 최소 권한을 적용한다. 하위 설정은 부모보다 낮게 저장할 수 없으며 프론트 선택지도 제한한다. 기존 하위 설정이 낮더라도 실제 권한은 조상 제한을 따른다.
- 태그는 문서 생성·수정 권한 검사 이후 같은 저장 트랜잭션에서 생성한다. 독립 생성 API는 제거했다.
- 최신 요청에 따라 문서 삭제는 관리자만 허용한다. 작성자 예외는 제거하고 보존·복구 기능은 프론트 TODO.md에 유지했다.
- 문서·이력·diff는 동급의 공개 열람 정책이다.
- 로그아웃 API는 현재 토큰을 DB에 영속 폐기한다. 다른 기기 로그인은 유지한다. 비밀번호 재설정은 모든 기존 액세스/MFA 토큰을 무효화한다.

## 수정한 문제

- 문서별 ACL·전역 최소 등급·카테고리 상속 제한을 함께 검사하며 관리자는 우선 통과한다.
- 이동 권한 없는 사용자의 카테고리 선택을 막고, 기존 비말단 카테고리 문서는 제자리 수정 가능하게 했다.
- 프론트에만 있던 관리자 본인 역할 변경 금지를 서버에도 적용했다.
- 공개 프로필과 관리자 사용자 목록을 필드 허용 목록으로 바꿔 2FA 내부 상태 등의 노출을 막았다.
- 관리자 화면이 로그아웃/권한 변경 후에도 남는 문제를 인증 이벤트와 창 포커스 재검사로 보완했다.
- 인증 완료된 사용자명·이메일만 알면 다른 브라우저에서도 가입할 수 있던 경로를 임의 비밀값으로 결합했다. 이메일 토큰마다 고유 ID도 부여한다.
- 2FA 활성화/해제에 IP당 분당 5회 제한을 추가했다.
- 비밀번호 재설정 후 기존 액세스/MFA 토큰이 살아 있던 문제를 session_version으로 해결했다.
- 문서 본문·이력·미리보기의 raw HTML을 sanitize하여 iframe/srcdoc·스크립트·이벤트 속성 등의 저장형 XSS 경로를 차단했다. 일반 마크다운·정렬·표·체크리스트는 유지한다.

## 유지한 특이점과 제한

- 초기 관리자 환경변수 계정은 재시작 시 관리자 권한으로 복구한다(사용자 확인).
- 일반 회원에게 허용된 카테고리에서의 신규 작성은 유지한다. 작성자 삭제 예외는 최신 요청으로 제거했다.
- 이메일 변경·2FA 설정 시작은 로그인 세션을 신뢰한다. 별도 비밀번호 재입력은 요구하지 않는다.
- 다중 프로세스 환경에서의 TOTP 동시 사용·요청 제한 공유는 운영 환경에서 별도 확인이 필요하다.
- sanitize로 위험한 HTML과 임의 스타일은 표시되지 않을 수 있다.

## 배포와 검증


- `permissions.rename`과 `wikiuser.session_version`은 서버 시작 시 없는 컬럼만 기본값과 함께 추가한다. 기존 문서 ACL·사용자 데이터와 이미 저장된 설정을 유지한다. README의 마이그레이션 항목 참고.
- 프론트와 백엔드를 함께 배포해야 한다. 이전 가입 화면은 새로고침하고 인증을 다시 요청한다.
- 프론트 프로덕션 빌드, 변경 소스 ESLint, 마크다운 보안 테스트 2개 통과. 번들 크기 경고는 남아 있다.
- npm audit에서 개발 도구 관련 기존 의존성 3건이 보고되었다. 이번 권한 수정에 의존성 전체 업데이트는 포함하지 않았다.
- 제목 편집 UI는 별도 요청 기능으로 계속 보류 상태다. 커밋·머지·푸시하지 않았다.


## 최종 검증 (코멘트 복구 제거 후)

- 백엔드 전체 pytest: 118 passed, 353 warnings.
- 프론트 프로덕션 빌드 성공, 변경 JS/JSX 및 MarkdownSecurity.js ESLint 통과, 마크다운 보안 테스트 2개 통과.
- 양쪽 git diff --check 통과. 기존 번들 크기 경고와 라이브러리 경고는 남아 있다.
- 운영 DB 변경·커밋·머지·푸시·배포는 수행하지 않았다. rename/session_version 컬럼과 revokedtoken 테이블의 운영 적용은 승인 대기다.
